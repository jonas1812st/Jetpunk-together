services:
  jetpunk-together:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - PORT=${PORT:-3000}
      - HOST_SERVER=${HOST_SERVER:-http://localhost:3000}
    volumes:
      - ./database:/app/database
    restart: unless-stopped
